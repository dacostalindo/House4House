# Wiki Schema (test fixture)

This is a minimal schema for the `seed` test fixture. Real schema lives at `wiki/CLAUDE.md`. This fixture exists so `/wiki-lint` has a small wiki to run against in the smoke test.

Page conventions: kebab-case filenames; YAML frontmatter with `last_verified` field; required sections per type. See real `wiki/CLAUDE.md` for the full spec.
