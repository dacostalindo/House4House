# Wiki reconcile report — 2026-05-22 18:00 local

Triggered by `/wiki-reconcile` immediately after PR-C (`silver_unified_developments` ship).

## Summary

- **6 findings** total
- **4 auto-fixed** (regex-only, no semantic change)
- **2 known-deviation re-logged** (already accepted 2026-05-15)
- **0 ADRs created**
- **0 stale pages**
- **0 unresolved wikilinks** (the 22 "unresolved" detections are intentional dbt-model code references)

## Schema agent — 2 findings (known deviations, no action)

Both were explicitly accepted at the 2026-05-15 reconcile (logged in `wiki/log.md`); the pages are domain-appropriate as-is and stay that way.

1. **`wiki/concepts/portal-field-map.md`** — concept page lacks the canonical `## Why` / `## How` sections. It is a *reference matrix* (lookup table for cross-portal column correspondences) rather than a pattern walkthrough; the table layout is the value.
2. **`wiki/sources/crus.md`** — source page lacks `## Source` / `## Schema` / `## Quirks` sections. It is a *retired source* (superseded by `[[crus-ogc]]`); the historical-layout content is the value.

If we ever want to bring these into schema compliance, both would need restructuring rather than additions — that is a documentation refactor, not a drift fix.

## Links agent — 0 actionable findings

22 "unresolved" detections, all benign:

- **9 dbt-model references** — `[[dim_geography]]`, `[[silver_sce_buildings]]`, `[[silver_unified_developments]]`, `[[parcel-universe]]` cited as load-bearing tables. These are code references; the dbt YAML is their source of truth. Acceptable as-is per the 2026-05-15 reconcile precedent.
- **6 structural self-references** — `[[CLAUDE.md]]`, `[[index.md]]`, etc. — intentional.
- **2 explicit-path links** — `[[planning/PoCs/agentic-pipeline]]` resolves correctly under the `wiki/<dir>/<name>` rule.
- **5 README.md detections** — no bare `[[README]]` usage; all are explicit with dir prefix.

The new `wiki/concepts/cross-portal-dev-dedup.md` uses `[[silver_unified_developments]]` and `[[silver_sce_buildings]]` intentionally to distinguish the table it describes from its sibling — consistent with the existing pattern.

## Reciprocity agent — 1 finding (auto-fixed)

**Scan 1 (ADR supersedes):** PASS — no supersession relationships declared across the 17 ADRs; graph is acyclic by construction.

**Scan 2 (sprint status-history regex):** FAIL on `wiki/sprints/sprint-08.md` lines 228-232. Four bullets used `2026-05-12 (am):` / `(pm):` / `(evening):` / `(late):` — the time-of-day parenthetical between the date and the colon broke the `^- \d{4}[-Q]\S*:\s+\S.*` regex.

**Fix applied:** moved the time-of-day token inside the content text:

```
- 2026-05-12 (am): restructured…       →  - 2026-05-12: (am) restructured…
- 2026-05-12 (pm): post-recovery…      →  - 2026-05-12: (pm) post-recovery…
- 2026-05-12 (evening): refactored…    →  - 2026-05-12: (evening) refactored…
- 2026-05-12 (late): added Activity 2… →  - 2026-05-12: (late) added Activity 2…
```

No semantic change. All four lines now match the regex.

## Freshness agent — 0 findings

Reference date 2026-05-22, 90-day cutoff 2026-02-21. Oldest `last_verified` across the wiki is 2026-05-05 (17 days old). Every typed page (sources, concepts, decisions, architecture, planning, use-cases) carries a `last_verified` date.

## Ingest agent — not run

PR-C (`feature/pr-c-silver-unified-developments`) was committed and pushed to `origin` but is not yet merged to `main`. No gstack merge to ingest from this session; the PR-C concept page (`cross-portal-dev-dedup.md`) already captures the design rationale that an ADR ingest would extract.

## Files touched

- `wiki/sprints/sprint-08.md` — 4 status-history bullet fixes (4 edits).
- `wiki/index.md` — `Last reconcile run:` bumped to 2026-05-22.
- `wiki/log.md` — reconcile entry appended.
- `wiki/lint-reports/2026-05-22T180000.md` — this report (new).
